<style>
    .complaint-card {
        background-color: #f9f9f9;
        padding: 20px;
        border: 1px solid #ddd;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .selected-text {
        margin-bottom: 20px;
    }

        .selected-text h5 {
            font-weight: bold;
            margin-bottom: 10px;
        }

    #complaint-text {
        width: 100%;
        height: 40px;
        padding: 10px;
        font-size: 16px;
        border: 1px solid #ccc;
        border-radius: 5px;
    }

    #selected-text-preview {
        font-size: 16px;
        color: #666;
    }

    .explanation {
        margin-bottom: 20px;
    }

        .explanation h5 {
            font-weight: bold;
            margin-bottom: 10px;
        }

    #complaint-explanation {
        width: 100%;
        height: 100px;
        padding: 10px;
        font-size: 16px;
        border: 1px solid #ccc;
        border-radius: 5px;
    }

    #submit-complaint {
        background-color: #4CAF50;
        color: #fff;
        padding: 10px 20px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
    }

        #submit-complaint:hover {
            background-color: #3e8e41;
        }
</style>
<br />
<br />
<br />
<br />
<br />
<br />
<div class="complaint-card">
    <div class="selected-text">
        <h5>Şikayet Edilecek Metin</h5>
        <select id="complaint-text" class="form-control">
            <option value="">Lütfen şikayet edilecek metni seçin</option>
            <option value="text1">Text 1</option>
            <option value="text2">Text 2</option>
            <option value="text3">Text 3</option>
        </select>
        <br />
        <select id="complaint-text" class="form-control">
            <option value="">Lütfen şikayet edilecek metni seçin</option>
            <option value="text1">Text 1</option>
            <option value="text2">Text 2</option>
            <option value="text3">Text 3</option>
        </select>
        <p id="selected-text-preview"></p>
    </div>
    <div class="explanation">
        <h5>Şikayet Açıklaması</h5>
        <textarea id="complaint-explanation" class="form-control" placeholder="Lütfen şikayet açıklamanızı girin"></textarea>
    </div>
</div>

<!-- Button to submit complaint -->
<button id="submit-complaint" class="btn btn-primary" style="margin-block-end: 10px;margin-left: 1100px;margin-top: 10px;">
    Şikayet Gönder
</button>

<!-- JavaScript code to handle complaint submission -->
<script>
    const complaintTextSelect = document.getElementById('complaint-text');
    const selectedTextPreview = document.getElementById('selected-text-preview');
    const complaintExplanation = document.getElementById('complaint-explanation');
    const submitComplaintButton = document.getElementById('submit-complaint');

    complaintTextSelect.addEventListener('change', (e) => {
      const selectedText = e.target.value;
      selectedTextPreview.textContent = selectedText;
    });

    submitComplaintButton.addEventListener('click', () => {
      const complaintText = complaintTextSelect.value;
      const explanation = complaintExplanation.value;

      // Send complaint data to server or handle it as needed
      console.log(`Şikayet metni: ${complaintText}`);
      console.log(`Şikayet açıklaması: ${explanation}`);
    });
</script>